# https://kubernetes.io/docs/concepts/workloads/controllers/deployment/
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rl-deployment
  labels:
    app: rl-deployment
spec:
  selector:
    matchLabels:
      app: rl-deployment
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container:  rl-deployment
      labels:
        app: rl-deployment
    spec:
      nodeSelector:
        kubernetes.io/hostname: ocr1 # Since it uses hostPath

      # initContainers:
        # Init containers are exactly like regular containers, except:
          # - Init containers always run to completion.
          # - Each init container must complete successfully before the next one starts.
      containers:
      - name: rl-deployment
        image: ghcr.io/yoyozbi/niles:v1.1 #{"$imagescan": "niles-scan"}
        imagePullPolicy: IfNotPresent
        resources:
          requests:
            cpu: 100m
            memory: 150Mi
          limits:
            cpu: 100m
            memory: 150Mi
        volumeMounts:
        - name: config-rl
          mountPath: /usr/src/niles/config
        - name: stores-rl
          mountPath: /usr/src/niles/stores
        - name: svc
          mountPath: /svc
      volumes:
        - name: stores-rl
          hostPath:
            path: /data/niles/rl
        - name: config-rl
          secret:
            secretName: config-rl
        - name: svc
          secret:
            secretName: svc
      restartPolicy: Always
