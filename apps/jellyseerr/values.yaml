replicaCount: 1

image:
  repository: ghcr.io/fallenbagel/jellyseerr
  pullPolicy: IfNotPresent

service:
  main:
    enabled: true
    type: ClusterIP
    ports:
      http:
        port: 80
        targetPort: 5055

