apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: vikunja
spec:
  interval: 5m
  chart:
    spec:
      chart: vikunja
      version: '2.0.0'
      sourceRef:
        kind: HelmRepository
        name: vikunja
  values: {}
