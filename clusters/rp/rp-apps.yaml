apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: rp-apps
  namespace: flux-system
spec:
  interval: 10m
  sourceRef:
    kind: GitRepository
    name: fleet-repo
  path: ./clusters/rp/overlays/apps
  prune: false
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age
